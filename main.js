function loadPanel(panel) {
    if(panel === "systemTools") {
        window.location.href = panel + ".html";
    }
    if(panel === "dashboard") {
        window.location.href = "index.html";
    }
    // TODO
    if(panel === "admin") {

    }
    if(panel === "userlogin") {

    }
}